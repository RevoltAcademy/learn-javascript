/**
 * 布林 Boolean
 * ================
 *
 * 正如呢球波, 一係入, 一係唔入
 * 布林值也一樣, 只有兩種: true & false (對與錯)
 *
 */

var popo_is_murderer = false;

while (popo_is_murderer) {
    console.log("報仇");
}

/**
 * while是一種循環式。
 * 只要條件是對, 就可以不斷循環執行區塊中的代碼。
 *
 * 所以從上例看到,
 * 只要 popo_is_murderer 這變數的值為真, "報仇"字串會不斷出現。
 *
 */
