/**
 * Type of Value
 * ======================
 * 
 * What is the type of "123"?
 */
const type = ""; // enter the type below
console.log(`Your answer is ${typeof "123" === type ? "correct" : "wrong"}.`);
