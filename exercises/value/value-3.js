/**
 * Type of Value
 */

//  What is the type of "apple"?
(() => {
    // enter the type below
    const type = "";
    console.log(`Your answer is ${typeof "apple" === type ? "correct" : "wrong"}.`);
})();
