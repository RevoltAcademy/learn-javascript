/**
 * Function with Parameters
 * ======================
 * The parentheses of a Function may include parameter names separated by commas:
 *  (parameter1, parameter2, ...)
 *
 * Resolve below error
 */

const num = 1;

function multiplyBy3() {
    return val * 3
}

console.log(`The value of "num" multiplied by 3 = ${multiplyBy3(num)}`)