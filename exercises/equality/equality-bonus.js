/**
 * Equality
 * =========================
 * NaN -> Not-A-Number which type is number (try "console.log(typeof NaN)").
 * However, NaN is a property of the global object.
 * 
 * What is the result of NaN == NaN?
 */
const result = ; // answer with "true" / "false"


console.log(`Your answer is ${NaN == NaN === result && typeof result === "boolean" ? "correct" : "wrong"}.`);
