/**
 * Strict Equality (===)
 * =========================
 * 
 * What is the result of "abc" === "abc"?
 */
const result = ; // answer with "true" / "false"

console.log(`Your answer is ${"abc" === "abc" === result && typeof result === "boolean" ? "correct" : "wrong"}.`);
