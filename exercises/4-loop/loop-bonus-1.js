/**
 * For/Of Loop
 * ======================
 * Syntax:
 * for (variable of iterable) {
 *      // code block to be executed
 * }
 * 
 * variable = 在每個loop中, 下一個屬性的值 (value of the next property) 都會被放進 (assign) variable中 
 * p.s. variable 可以以 const, let, 或 var 來宣告 (declaration).
 * 
 * iterable = 一個物件 (Object) 擁有迭代 (iterable) 的特性. i.e. things that can be looped
 * p.s. Iterable Objects = Arrays, Strings, Maps, NodeLists, Set, and more.
 *
 * ----------------------
 *
 * So, here is the little quest:
 *
 * Please use the console.log to print out expected output by for/of loop
 * 請用 "console.log" + for/of loop 顯示出下列結果
 * 
 * Expected output: 
 * 2
 * 4
 * 6
 * 10
 * 12
 * 14
 * 
 */

const arr = [1, 2, 3, 5, 6, 7];

