/**
 * Remainder (%)
 * ======================
 * The remainder operator returns the remainder left over 
 * when one operand is divided by a second operand.
 * It always takes the sign of the dividend.
 * 
 * Operator: var1 % var2
 *
 * What is the result of 12 % 5?
 */
const result = ; // assign the value to the result
console.log(`Your answer is ${(12 % 5) === result ? "correct" : "wrong"}.`)