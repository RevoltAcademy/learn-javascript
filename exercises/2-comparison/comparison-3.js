/**
 * Less than operator (<)
 * ======================
 * The less than operator returns true if the left operand is less than the right operand.
 * 
 * Syntax: x < y
 *
 */

//  What is the result of 3 < 4?
const result = ;
console.log(`Your answer is ${(3 < 4) === result ? "correct" : "wrong"}`);
