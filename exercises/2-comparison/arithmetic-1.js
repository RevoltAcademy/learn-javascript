/**
 * Addition
 * ======================
 * The addition operator produces the sum of numeric operands or string concatenation.
 * Operator: x + y
 * 
 * What is the result of 1 + 1?
 */
const result = ;
console.log(`Your answer is ${(1 + 1) === result ? "correct" : "wrong"}.`)