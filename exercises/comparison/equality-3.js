/**
 * Equality (==/===) on Object Reference
 * =========================
 */

/**
 * Q1: What is the result of { data: 123 } == { data: 123 }?
 */
const result = ; // answer with "true" / "false"

console.log(`Your answer is ${({ data: 123 }) == ({ data: 123 }) === result && typeof result === "boolean" ? "correct" : "wrong"}.`);


/**
 * Q2: What is the result of a == b?
 */
const a = { data: 123 };
const b = a;

const result2 = ; // answer with "true" / "false"

console.log(`Your answer is ${a == b === result2 && typeof result2 === "boolean" ? "correct" : "wrong"}.`);
