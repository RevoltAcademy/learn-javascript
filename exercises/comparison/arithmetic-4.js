/**
 * Division (/)
 * ======================
 * The division operator produces the quotient of its operands
 * where the left operand is the dividend and the right operand is the divisor.
 * Operator: x / y
 *
 * What is the result of 2.0 / 0.0?
 */
const result = ; // assign the value to the result
console.log(`Your answer is ${(2.0 / 0.0) === result ? "correct" : "wrong"}.`)