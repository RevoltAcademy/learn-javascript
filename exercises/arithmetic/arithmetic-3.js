/**
 * Multiplication (*)
 * ======================
 * The multiplication operator produces the product of the operands.
 * Operator: x * y
 * 
 * What is the result of -2 * 2 ?
 */
const result = ;
console.log(`Your answer is ${(-2 * 2) === result ? "correct" : "wrong"}.`)