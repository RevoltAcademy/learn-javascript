/**
 * Decrement (--)
 * ======================
 * - If used postfix, with operator after operand (for example, x--), then it decrements and returns the value before decrementing.
 * - If used prefix, with operator before operand (for example, --x), then it decrements and returns the value after decrementing.
 * 
 * Operator: x-- or --x
 *
 */
const x = 3;
const y = x--;
const z = --x;

// What is the value of y?
const result = ; // assign the value to the result
console.log(`Your answer is ${y === result ? "correct" : "wrong"}.`)

// What is the value of x?
const result2 = ; // assign the value to the result
console.log(`Your answer is ${z === result2 ? "correct" : "wrong"}.`)