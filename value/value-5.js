/**
 * Type of Value
 */

//  What is the type of true?
(() => {
    // enter the type below
    const type = "";
    console.log(`Your answer is ${typeof true === type ? "correct" : "wrong"}.`);
})();
