/**
 * Type of Value
 */

//  What is the type of undefined?
(() => {
    // enter the type below
    const type = "";
    console.log(`Your answer is ${typeof undefined === type ? "correct" : "wrong"}.`);
})();
