/**
 * Type of Value
 */

//  What is the type of 1.3?
(() => {
    // enter the type below
    const type = "";
    console.log(`Your answer is ${typeof 1.3 === type ? "correct" : "wrong"}.`);
})();
