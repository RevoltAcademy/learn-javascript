/**
 * Type of Value
 */

//  What is the type of null?
(() => {
    // enter the type below
    const type = "";
    console.log(`Your answer is ${typeof null === type ? "correct" : "wrong"}.`);
})();
